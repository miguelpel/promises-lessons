let endpoint = 'http://api.nobelprize.org/v1/prize.json?';

// when he clicks ont the button get the imput from the user

// add en event listener on the button.
// this event listener will call a funtion
// this function needs to return me a string


// create the XMLHttp object.

// open the object with GET, the url of the endpoint, and true of false

// send the request

// listen to change in the readyState of the object when change => call a function

// function will check if readyState is 4 and if status is 200
// JSON.parse the responseText
// change my content ('output') with the text I want